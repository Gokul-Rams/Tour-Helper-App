<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project4148">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Find Tour"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DestinationDetailActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity
            android:name=".GuideDetailsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".GuidesigninActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".AdddetailsActivity"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".SettingActivity"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".HelpActivity"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".FinalDestinationActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>